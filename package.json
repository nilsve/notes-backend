{
  "name": "crm-backend",
  "scripts": {
    "deploy": "serverless deploy --aws-profile private",
    "remove": "serverless remove"
  },
  "devDependencies": {
    "serverless": "^1.74.1",
    "serverless-rust": "github:rust-serverless/serverless-rust#master"
  }
}
